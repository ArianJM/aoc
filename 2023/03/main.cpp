#include <iostream>
#include <fstream>
#include <string>
#include <regex>
using namespace std;

int getSumPartNumbers(string fileName) {
    ifstream input (fileName);
    int sum = 0;

    if (input.is_open()) {
        
    }
}

int main() {
    const << "Result: " << getSumPartNumbers("input") << endl;
}
